# VishGuard runtime dependencies.
#
# Floors are chosen to align with the Colab 2026-04 runtime (Python 3.12.13):
#   transformers 5.0.0, torch 2.10.0+cu128, torchaudio 2.10.0+cu128,
#   librosa 0.11.0, soundfile 0.13.1, pydantic 2.12.3, datasets 4.0.0,
#   scikit-learn 1.6.1, matplotlib 3.10.0, pytest 8.4.2, accelerate 1.13.0,
#   python-dotenv 1.2.2.
#
# Packages NOT preinstalled on Colab (must pip install): streamlit, jiwer.
# GPU-only extras live in requirements-gpu.txt (bitsandbytes for 4-bit LLM).

transformers>=4.44,<6
accelerate>=0.33
torch>=2.2
torchaudio>=2.2
librosa>=0.10
soundfile>=0.12
pydantic>=2.7
streamlit>=1.37
jiwer>=3.0
datasets>=2.20
scikit-learn>=1.5
python-dotenv>=1.0
matplotlib>=3.8
pytest>=8.2
