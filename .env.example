# Optional — only needed if the Streamlit UI is run with a hosted LLM
# instead of the default open-HuggingFace LLM path. The academic-rubric
# path uses only open models and does not require any of these keys.

# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# HF_TOKEN=
